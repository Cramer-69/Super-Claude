# Lightweight dependencies for cloud deployment (Render/Cloud Run).
# The app detects the cloud env and runs MinimalConductor, which only needs
# a web server plus the provider SDKs. Heavy local-only deps (chromadb,
# sentence-transformers, langchain) are intentionally excluded for fast,
# reliable builds. For full local mode use requirements.txt instead.
fastapi>=0.109.0
uvicorn>=0.27.0
gunicorn>=21.2.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
rich>=13.7.0
jinja2>=3.1.3
aiofiles>=23.2.0
httpx>=0.26.0
requests>=2.31.0
openai>=1.10.0
anthropic>=0.18.0
boto3>=1.34.46
google-generativeai>=0.3.0

# Optional plugins. Both are pure-Python HTTP clients and stay inert
# unless configured (MEM0_API_KEY / MEM0_ENABLED, FIRECRAWL_API_KEY),
# so they are safe to ship in the lean cloud image.
mem0ai>=0.1.0
firecrawl-py>=4.0.0
