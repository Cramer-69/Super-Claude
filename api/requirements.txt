# Vercel serverless function dependencies.
#
# Vercel's @vercel/python builder installs the requirements.txt located next
# to the entrypoint (this file) instead of the heavy root requirements.txt,
# which pins sentence-transformers (PyTorch, ~1 GB) + chromadb + langchain
# and would blow past Vercel's 250 MB serverless function limit. Reference
# the shared light dependency set instead of duplicating it, so the two
# lists can't drift out of sync.
-r ../requirements-cloud.txt
